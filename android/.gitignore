# Build outputs and machine-local settings.
build/
.gradle/
local.properties
.idea/

# Release signing material, generated by scripts/package_android.py on the
# first release build.  Losing release.jks means a brand-new app signature
# forever -- back it up somewhere safe, but never commit it.
keystore/
