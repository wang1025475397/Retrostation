# 更新日志

## 0.3.0 — 2026-09-05

### 新增

- **设置菜单新增两行**（按 START 打开，位于底部）：
  - **「缩略图缓存」** —— 开关。关掉后不再读写任何缓存文件，封面改为实时缩放。
    桌面端实测每图 5~10 ms，几乎无感；掌机明显变慢，建议保持开启。
  - **「清空图片缓存」** —— 删除卡上所有 `.cache` 目录回收空间，完成后提示删了多少个文件。
- **缩略图自动清理**：启动后的后台扫描会顺带清掉失效条目（封面已更换、旧版本残留），
  被识别不到的缓存文件不再永久占着卡。

### 修复

- **副屏视频完全不出画面**（44 / 183 等机型）。根因是固件的两个问题叠加：
  1. 固件每次重启都把 `libfontconfig.so.1` 指回旧版，缺 `FcWeightFromOpenTypeDouble`，
     ffmpeg 一启动就 `symbol lookup error`；
  2. 固件的绕法是给播放器一套私有库路径，但那条路径上的 libavformat 是**精简版**
     ——只有 10 个 muxer 且全是音频容器，**没有 `rawvideo`**，于是每个视频都报
     `Requested output format 'rawvideo' is not a suitable output format`。
  现在启动器每次启动会检查并修好 fontconfig 链接（只在确实缺符号时才动，各架构目录
  各自用自己的新版本），解码器进程则使用干净的运行环境。**详见 USAGE 第十四节。**
- **换封面后仍显示旧缩略图**：缓存文件名加入了文件修改时间，换图立即生效。
- **封面透明通道丢失**：Logo 不再被压平成灰底，网格与轮播里的透明 PNG 保持透明。
- **桌面端封面只占格子中间一小块**：等比缩放现在允许放大，小封面也会撑满格子。
- **半截缓存文件导致崩溃**：缩略图改为先写临时文件再改名（原子写入）；清理时若文件
  被占用（Windows 常见）不再抛异常终止绘制。
- **ffprobe 探测挡住了首帧**：探测时长实测 4.8 秒，超过 3 秒的僵死判定，导致视频被
  永久拉黑。现在探测挪到首帧之后、独立线程执行。
- **音频重试挡住了解码线程**：声卡被占用时重试要 0.7 秒，原本排在解码线程之前。
  现在音频在后台线程启动；同时修好了切换游戏时音频可能泄漏、在模拟器启动后继续响的问题。
- **ffmpeg 可用性检测过粗**：原来只查 `which ffmpeg`，二进制坏了也会反复启动它。
  现在会真正执行一次版本探测。

### 外观

- **网格与轮播改为等比完整显示**：封面不再被裁剪，而是按自身比例取最大尺寸居中。
  格子大小保持不变，方形（GBA）、横向（FC）、纵向（MD）封面都能完整显示。
- 去掉网格卡片的常驻边框与底板，选中项保留高亮描边；轮播卡片也不再有底板。

### 其他

- 缩略图编码质量 86 → 80，体积约减 25%，缩略图尺寸下看不出差别。
- 解码器出错时会把 ffmpeg 的报错原文、实际执行的命令、解析到的二进制与 muxer 数量
  一并记进 `log.txt`，便于自行排查。
- 文档：新增「缩略图缓存」「副屏视频不出画面」两节（中英文）。

---

## 0.2.1 及更早

早期版本未维护变更记录。
